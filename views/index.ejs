<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="css/style.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="table-container">
      <h2 class="todo-title">Todo List</h2>
      <div>
        <a class="btn" href="/add-todo">Add Todo</a>
      </div>
      <br />
      <table>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th>Created At</th>
          <th>Update At</th>
          <th>Action</th>
        </tr>
        <tr>
          <% for(let i=0; i<todos.length; i++){ %>
          <td><%= todos[i].title  %></td>
          <td><%= todos[i].desc %></td>
          <td><%=moment(todos[i].createdAt).format("MMM DD, YYYY,HH:mm:ss")  %></td>
          <td><%= moment(todos[i].updatedAt).format("MMM DD, YYYY,HH:mm:ss")  %></td>
          <td>
            <a class="btn" href="/update?id=<%= todos[i]._id %>">Edit </a>
            <a class="btn" href="/delete?id=<%= todos[i]._id %>">Delete</a>
          </td>
        </tr>
          <% } %>
        
      </table>
    </div>
  </body>
</html>
